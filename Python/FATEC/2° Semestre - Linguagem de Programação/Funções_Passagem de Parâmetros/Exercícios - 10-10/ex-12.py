# Crie uma função chamada calculaPotencia que recebe dois números inteiros como parâmetros, base e expoente, e retorna a base elevada ao expoente

def calculaPotencia(a, x):
    resultado = a ** x
    return resultado

print(calculaPotencia(a=2, x=5))
