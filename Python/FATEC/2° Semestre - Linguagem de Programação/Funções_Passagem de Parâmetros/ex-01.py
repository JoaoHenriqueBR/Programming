# Crie uma função chamada soma que recebe dois inteiros como parâmetros e retorna a soma deles

def soma(a, b):
    soma = a + b
    return soma

print('Soma: {}'.format(soma(a=int(input('Digite um número: ')), b=int(input('Digite outro: ')))))
